apiVersion: v1
kind: Service
metadata:
    name: "tothemoon-3767"
    labels:
        app: "tothemoon-3767"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "tothemoon-3767"